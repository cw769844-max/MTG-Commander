# Only used by the Vite dev server, which runs on its own port and therefore
# needs to be told where the API lives. The production build is served by the
# API server itself, so it talks to its own origin and needs nothing here.
VITE_API_BASE=http://localhost:4000
